# Copy this file to .env (or .env.local) and adjust the value for your
# local setup. Real credentials are never committed to this repository.
#
# Base URL of the BOZZ backend API. Must include the /api prefix.
VITE_API_BASE_URL=http://localhost:3000/api
